# KrishiSaathi AI — backend (hackathon / free-tier stack)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
tzdata==2026.2
# IANA tz database for zoneinfo on Windows (e.g. Asia/Kolkata in api/routes/weather.py).
# Supabase Auth + REST + RPC use httpx (no supabase-py; avoids heavyweight native transitive deps).
aiosqlite>=0.20.0
python-multipart>=0.0.9

langgraph>=0.2.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0

chromadb>=0.5.0
pillow>=10.4.0

pytest>=8.3.0
pytest-asyncio>=0.24.0
respx>=0.21.0
